# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 코딩 행동 지침

LLM의 일반적인 코딩 실수를 줄이기 위한 행동 지침입니다. 프로젝트별 지침이 있을 경우 본 가이드라인과 병합하여 사용합니다.

**트레이드오프:** 본 지침은 속도보다 신중함에 우선순위를 둡니다. 사소한 작업은 상황에 맞게 판단합니다.

### 1. 구현 전 사고 (Think Before Coding)

가정하지 않습니다. 모호함을 숨기지 않습니다. 트레이드오프를 명확히 밝힙니다.

구현을 시작하기 전 다음을 준수합니다:

- 자신의 가정을 명시적으로 기술합니다. 불확실한 경우 질문합니다.

- 해석의 여지가 여러 가지라면 임의로 선택하지 말고 대안들을 제시합니다.

- 더 간단한 접근 방식이 있다면 제안합니다. 정당한 사유가 있다면 사용자의 요청에 반대 의견을 제시합니다.

- 불분명한 부분이 있다면 작업을 중단합니다. 혼란스러운 부분을 구체적으로 언급하며 질문합니다.

### 2. 단순성 우선 (Simplicity First)

- 문제를 해결하는 최소한의 코드만 작성합니다. 추측에 기반한 코드는 배제합니다.

- 요청되지 않은 기능은 추가하지 않습니다.

- 일회성 코드를 위해 추상화 계층을 만들지 않습니다.

- 요청되지 않은 유연성이나 설정 가능성을 고려하지 않습니다.

- 발생 불가능한 시나리오에 대한 예외 처리를 하지 않습니다.

- 200줄의 코드를 50줄로 줄일 수 있다면 코드를 다시 작성합니다.

- "시니어 엔지니어가 보기에 이 코드가 지나치게 복잡한가?"라고 자문합니다. 그렇다면 단순화합니다.

### 3. 정밀한 수정 (Surgical Changes)

필요한 부분만 수정합니다. 본인이 만든 코드의 뒷정리만 수행합니다.

기존 코드를 편집할 때 다음을 준수합니다:

- 인접한 코드, 주석, 포맷을 임의로 개선하지 않습니다.
- 망가지지 않은 부분을 리팩토링하지 않습니다.
- 본인의 스타일과 다르더라도 기존 스타일을 따릅니다.
- 작업과 무관한 데드 코드를 발견하면 보고하되 직접 삭제하지 않습니다.

수정으로 인해 사용되지 않게 된 요소가 발생할 경우:

- 본인의 수정으로 인해 불필요해진 임포트, 변수, 함수는 제거합니다.
- 기존에 존재하던 데드 코드는 요청이 없는 한 그대로 둡니다.
- 테스트 기준: 변경된 모든 라인은 사용자의 요청사항과 직접적으로 연결되어야 합니다.

### 4. 목표 중심 실행 (Goal-Driven Execution)

성공 기준을 정의합니다. 검증될 때까지 반복합니다.

작업을 검증 가능한 목표로 변환합니다:

- "유효성 검사 추가" → "잘못된 입력에 대한 테스트 작성 후 통과 확인"
- "버그 수정" → "버그를 재현하는 테스트 작성 후 통과 확인"
- "X 리팩토링" → "리팩토링 전후의 테스트 통과 확인"

다단계 작업의 경우 간략한 계획을 수립합니다:

1. [단계] → 검증: [확인 사항]
2. [단계] → 검증: [확인 사항]
3. [단계] → 검증: [확인 사항]

성공 기준이 명확해야 독립적인 작업이 가능합니다. "작동하게 만들기"와 같은 모호한 기준은 불필요한 재질의를 야기합니다.

#### 지침 작동 확인

- Diff 내 불필요한 변경 감소
- 복잡성으로 인한 재작성 빈도 감소
- 구현 전 질문을 통한 명확한 의사결정 증대

---

## 프로젝트 개요

`skala-vue/` 디렉토리에 `create-vue`로 스캐폴딩된 Vue 3 + Vite 프로젝트입니다. Composition API(`<script setup>`, 함수형 Pinia 스토어)를 사용하며, 아직 초기 단계로 라우트, 컴포넌트, 테스트가 정의되어 있지 않습니다.

## 빌드/개발 명령어 (작업 디렉토리: `skala-vue/`)

- `npm install` — 의존성 설치
- `npm run dev` — 개발 서버 실행 (Vite)
- `npm run build` — 프로덕션 빌드
- `npm run preview` — 빌드 결과 미리보기
- `npm run lint` — ESLint + Oxlint 순차 실행 (`lint:eslint`, `lint:oxlint`), 둘 다 `--fix` 자동 적용
- `npm run format` — Prettier로 `src/` 포맷팅

테스트 프레임워크는 아직 구성되어 있지 않습니다 (Vitest/Cypress 등 없음, `test` 스크립트 없음).

## 아키텍처

- **스택**: Vue 3 (Composition API), Vue Router 5 (현재 `routes: []`로 비어 있음), Pinia (함수형 setup 스토어 방식), Vite. TypeScript 미사용 (JS + `jsconfig.json`).
- **경로 별칭**: `@` → `src/` (Vite, jsconfig에 동일하게 설정됨).
- **디렉토리 구조**: `src/main.js` (앱 부트스트랩, Pinia+Router 설치), `src/App.vue` (루트 컴포넌트, 현재 `<router-view>` 미연결), `src/router/index.js` (히스토리 모드 라우터, 라우트 미정의), `src/stores/counter.js` (Pinia 예제 스토어, 함수형 `defineStore` 문법).
- **린트/포맷**: ESLint (flat config) + Oxlint 이중 린터, Prettier로 포맷 담당 (세미콜론 없음, single quote, printWidth 100). ESLint 설정은 포맷 관련 규칙을 Prettier에 위임 (`eslint-config-prettier`).

## 강의자료 및 실습 가이드

이 프로젝트는 Vue.js 강의(`skala-vue/pdf/`에 원본 PDF 보관)를 따라가며 실습하는 용도입니다. PDF 내용을 정리한 문서:

- `skala-vue/docs/checklist.md` — **종합과제 체크리스트 (최우선 기준 자료)**. 제출 방식, 일자별 정확한 범위와 일정.
- `skala-vue/docs/vue-study-guide.md` — 챕터별 이론 정리 (Vue 개요, Directive, Composition API, Component 등)
- `skala-vue/docs/vue-practice-exercises.md` — `[실습]`/`Code Challenge`만 분리한 실습 목록. 실습 코드를 작성할 때는 이 문서를 먼저 참고할 것.

현재 확보된 PDF는 전체 10개 챕터 커리큘럼 중 **1~4장(Vue.js 시작하기 / Vue 문법 / Composition API / Vue Component)까지만** 다룹니다. 5장(Vue Router) 이후 자료가 추가되면 위 두 문서도 이어서 갱신해야 합니다.

이 강의의 핵심 실습은 하나의 **"날씨(Weather)" 대시보드 앱**이며, `checklist.md` 기준 실제 일정으로 점진적으로 발전합니다: 1일차(7/31) 정적 Mockup → 2일차(8/3) Composition API 적용 + 컴포넌트 4개 분리 → 3일차(8/4) Router·Pinia·Axios(실제 API 연동, ℃/℉ 토글) → 4일차(8/5) Element Plus·Modern JS 정리 및 빌드/배포. 실습 관련 작업을 할 때는 이전 단계 코드를 확장하는 흐름이라는 점을 염두에 둘 것 (새로 만드는 게 아니라 계속 이어서 발전시킴).

### 자료 우선순위: 체크리스트 > day{N}.pdf > 158p 이론 교재

세 종류의 자료가 있으며, 서로 내용이 다르면 **위에 있는 자료가 우선한다**:

1. **`skala-vue/docs/checklist.md`** (최우선) — 사용자가 제공한 종합과제 체크리스트. 제출 방식·마감·일자별 정확한 범위를 규정하는 가장 신뢰할 수 있는 기준.
2. **`skala-vue/pdf/day{N}.pdf`** — 강사가 매일 제공하는 그날의 실습 강의안. 세부 구현 방법과 코드 스니펫을 제공하지만, 제출 방식 등 상위 정보는 체크리스트가 우선.
3. **158p 이론 교재** (`vue-study-guide.md`/`vue-practice-exercises.md`의 원본) — 배경 이론과 원본 문제.

**실제 제출 방식 (checklist.md 기준, 확정)**: **본인 GitHub 계정에 Public 저장소 생성**(`skala-vue`)하여 제출한다. 시크릿 창(⌘+Shift+N)으로 로그인 없이 소스가 보이는지 확인이 필수다.
- 현재 저장소: `https://github.com/wodnjs2020136144/skala-vue` (Public 전환 완료, 로그인 없는 요청으로 200 응답 확인함).
- ~~이전에 "이 분반은 Slack PDF 제출"이라고 판단했던 것은 착오였다~~ — 체크리스트로 GitHub Public 저장소 제출이 맞다는 것이 확인됨. 문서에 남아있는 관련 서술은 정정 완료.
- 4일차 종합과제는 새로 만드는 게 아니라 **1~3일차 결과물을 이어서 발전시킨 것**을 제출한다. 매일 커밋/푸시가 빠지면 안 됨.
- OpenWeatherMap API Key는 3일차(Axios 연동)에 필요하며, **계정 가입은 사용자가 직접 해야 하는 작업**(Claude가 계정 생성을 대신할 수 없음)이므로 미리 안내만 하고 실제 가입/키 발급은 사용자에게 요청한다.
- **평가 유의사항** (`checklist.md` 하단 "점수가 갈리는 지점" 참고): Private 저장소는 감점(61~70점 구간)되므로 항상 Public 유지. 동일하게 동작해도 주석·변수명·작은 개선 등 직접 손댄 흔적이 보이면 가산점이므로, 코드를 작성할 때 의미 있는 변수명과 필요한 주석을 신경 쓰고, 요청받지 않은 개선(부가 실습 등)도 적극적으로 제안한다.

**새 `day{N}.pdf`를 받으면 다음 순서로 처리한다**: (1) pdf skill로 전체 내용 읽기 → (2) `checklist.md`/기존 문서/해당 `day{N}.md`와 비교해 차이점을 분석 → (3) 구체적인 수정 방안을 사용자에게 제시하고 승인받은 뒤에만 문서에 반영한다.

## 실습 기록 규칙

이 강의는 4일 과정이며, 매일 실습이 누적되어 마지막 날 하나의 완성된 결과물이 나오도록 설계되어 있다. 과정 종료 시 전체 실습 과정을 정리한 보고서를 제출해야 한다. 이를 위해 다음 규칙을 따른다:

- 실습 관련 작업(코드 작성, 디버깅/트러블슈팅, 실행 및 테스트 등)을 하나 완료할 때마다, **사용자가 별도로 요청하지 않아도** `skala-vue/docs/reports/day{N}.md` (진행 중인 날짜)에 아래 형식으로 자동 기록한다:
  - **요구사항** — 무엇을 하려고 했는지
  - **사고 과정** — 어떻게 접근할지 어떤 판단을 했는지
  - **해결 과정** — 실제로 어떻게 구현/처리했는지. 각 단계가 파일을 새로 작성하거나 수정하는 단계라면, 그 단계 설명 바로 아래에 파일 경로와 함께 해당 코드 블록을 포함한다 (코드를 별도 섹션으로 몰아서 넣지 않고, 그 파일을 다룬 단계 안에 개별적으로 넣는다).
  - **트러블슈팅** — 문제/원인/해결 (문제가 없었으면 "없음"으로 기록)
  - **결과** — 최종적으로 어떻게 됐는지 (동작 확인, 스크린샷 등)
  - **느낀점** — 이 작업을 통해 배우거나 느낀 점
- 어느 날짜(day1~day4) 파일에 기록할지 세션 맥락만으로 판단하기 어려우면 사용자에게 확인한다.
- Day 4 실습이 마무리되면 `skala-vue/docs/reports/final-report.md`(최종 종합 보고서 템플릿)를 day1~day4 기록을 바탕으로 완성해야 한다.

### 스크린샷 관리

- 저장 위치: `skala-vue/docs/reports/images/day{N}/`
- 파일명 규칙: `{2자리 순번}-{작업슬러그}.확장자` (예: `01-dev-server-running.jpg`). 순번은 해당 day{N}.md 안의 기록 항목 순서와 일치시킨다.
- 사용자가 실습 결과 화면을 캡처해서 전달하면, 규칙에 맞는 파일명으로 저장하고 해당 항목의 **결과** 섹션에 `![설명](./images/day{N}/파일명)` 형태로 삽입한다.
- 최종 보고서(`final-report.md`) 제출 시 스크린샷도 함께 필요하므로, 이 이미지들은 삭제하지 않고 누적 보관한다.
